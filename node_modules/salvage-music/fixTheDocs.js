const fs = require("fs-extra");
fs.copySync(
  `./docs/salvage-music/${require("./package.json").version}`,
  `./docs`
);
fs.rmdirSync(`./docs/salvage-music/${require("./package.json").version}`, {
  recursive: true,
});
fs.rmdirSync(`./docs/salvage-music`);
